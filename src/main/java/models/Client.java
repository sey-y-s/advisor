package models;

import models.enums.Niveau;
import models.enums.Role;

public class Client {
    public Client(String nom, String prenom, String email, String motDePasse, Niveau niveau, int i, Object o, Integer telephone, Role role) {
    }
}
